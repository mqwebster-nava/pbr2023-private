
describe("Data Validation", () => {
    it("renders a test", () => {
      // check if all components are rendered
      expect("1").toBe("1");
    });
    it("renders another", () => {
        // check if all components are rendered
        expect("1").toBe("2");
      });
});